name: '[CI] Continuous Integration'

# Quando rodar?
on:
  pull_request:
    types: [opened, synchronize]

#Quais tarefas(jobs)?
jobs:
  #Tarefa
  lint:
    name: 'Lint'
    runs-on: ubuntu-latest
    steps:
      - name: 'Lint'
        run: 'npm run lint'
